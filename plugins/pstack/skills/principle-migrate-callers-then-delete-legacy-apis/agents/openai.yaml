interface:
  display_name: "principle-migrate-callers-then-delete-legacy-apis"
  short_description: "Run this pstack workflow only when explicitly invoked."
policy:
  allow_implicit_invocation: false
